#!/usr/bin/env bash

# There's nothing to do here, since we can't start dogstatsd from here.


# echo "datadog decorate"
# echo "check it out"
#
# ls
#
# # DATADOG_DIR="/home/vcap/app/datadog"
# BIN_DIR=$(cd $(dirname $0); pwd)
# ROOT_DIR=$(dirname $BIN_DIR)
# BUILD_DIR=$1
# DATADOG_DIR="$BIN_DIR/datadog"
#
# if [ -z ${DATADOG_API_KEY+x} ]; then
#   echo "Datadog API Key not set, not starting Datadog"
#   exit 0
# fi
#
# cd $DATADOG_DIR
#
# export log_file=$DATADOG_DIR/dogstatsd.log
# export api_key=$DATADOG_API_KEY
#
# ./dogstatsd start
